  imageScans:
  - policy:
      semver:
        range: "*"
    image: "ghcr.io/amundead/phplogin"
    tagName: phploginscan
    secretRef:
      namespace: application-testing
      name: phplogin
    interval: 1m
